"""Generator of the 4 x 4 quadrant board (ADR 0010).

Spirals, escapes, camp LEDs, distribution and the front-end strip of
one quadrant, computed from config/board.yaml. Four identical boards
tile the 8 x 8 play area; the right-hand pair is mounted rotated by
180 degrees, which leaves the LED diagonal unchanged.
"""
